# Runtime & Cache
cache/
paste-cache/
file-history/
shell-snapshots/
session-env/
debug/
ide/
statsig/
telemetry/
todos/
plugins/
projects/
plans/

# History & Stats
history.jsonl
stats-cache.json
*.log

# Local settings overrides
settings.local.json
*.local.json
*.local.md

# Private / machine-specific
chrome/
tasks/
skills/china-product-researcher/references/company-info.md

# Retro session digests (private project detail — keep local, never commit)
retro/

# Runtime state, backups & caches (machine-local — never commit, esp. to a public repo)
backups/
security/
sessions/
daemon/
jobs/
image-cache/
gh-pr-status-cache.json
security_warnings_state_*.json
.last-cleanup
.last-update-result.json

# Business-specific skill — keep fully private (workflow + real supplier contacts)
skills/whatsapp-supplier-responder/

# Daemon runtime state at root (machine-local)
daemon.lock
daemon.status.json
uploads/

# Machine-local sync helper & MCP auth cache (never commit)
sync.sh
mcp-needs-auth-cache.json

# Skill-optimizer run output (transient build artifacts, not a skill — keep local)
skills/search-first-workspace/
